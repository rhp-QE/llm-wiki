# 从个人笔记到活知识系统：用 Agent、约束与模板构建 llm-wiki

## 分享主题概览

这次分享从一个个人笔记库的改造开始，讲如何用 Agent、约束与模板，把零散文本转化为可持续生产、可治理、可消费的“活知识”。

核心问题不是“如何多存一些文档”，而是：

- 如何让 600+ 篇个人笔记重新产生价值？
- 如何让 Agent 不只是做 RAG，而是提炼概念、关系和长期模式？
- 如何把个人 wiki 的方法迁移到复杂业务场景？
- 如何把手工作坊式的 wiki 构建，升级成可规模化复制的知识生产线？

本次分享分为三个章节：

1. **个人知识库构建**：结合 llm-wiki，将普通个人笔记库升级为具备深度检索能力的结构化知识系统。
2. **业务落地**：将个人知识库的方法论迁移到数据场景，通过定义业务概念、约束和模板，构建可生产、可治理的专业领域知识系统。
3. **规模化设想**：通过 SKILL 体系、路由与质量保障，将 wiki 工程从手工调优升级为可复制的知识生产线。

---

## 第一章：让 600+ 笔记重获新生

### 1. 起点：写了十多年，却很少再读

我从 2013 年开始写日记，到现在已经积累了 600 多篇。它们看起来是一笔很宝贵的资产，但现实是：写完之后基本没有再系统读过。

最开始我尝试过 NotebookLM 这类工具。它可以从笔记里 RAG 出很多内容，但我发现一个关键缺口：它能回答“哪里写过什么”，却很难回答“这些经历说明我是一个什么样的人”。

比如：

- 我是个什么样的人？
- 我在追求什么？
- 哪些事情长期对我重要？
- 哪些人、事件、观念持续影响我？

这些问题不是简单检索能解决的。它们需要抽象、建模、归纳和长期关系追踪。

llm-wiki 出现后，我感觉它更接近这个问题的答案：它不只是帮我找文档，而是帮我把个人经历转化成一个持续演化的知识系统。

### 2. 第一阶段：把个人笔记导入 Obsidian

最初的输入很简单：600+ 篇 Markdown 日记。

这个阶段的问题也很明显：

- 只有纯文档输入，没有关联建模。
- 每篇笔记都是孤立节点。
- 很多重要人物、事件、主题只存在于原文里，没有被结构化沉淀。

图片占位：

- 原始个人知识库截图
- Obsidian 中零散节点截图

### 3. 第二阶段：让 Codex 基于 llm-wiki 思想自行建模

我给 Codex 的初始指令大致是：

> 参考 llm-wiki.md 文件，这是 Karpathy 对个人知识库的最新理解。请整理我的个人笔记知识，笔记都在 ./out 目录下，整理后的内容放到 ./wiki 目录下。

这个阶段开始有明显进展：

- wiki 目录初具规模。
- 一批重要概念、人物、故事线被挖掘出来。
- 原本分散在日记里的长期主题开始浮现。

图片占位：

- 初步 wiki 目录截图
- 初步抽取出的概念与故事线截图

但问题也非常明显：Agent 会偷懒。

典型问题包括：

- **人物丢失**：很多人生中重要人物没有进入实体库，例如“奶奶”。
- **描述过短**：实体页描述非常简略，没有体现长期关系和影响。
- **概念分类混乱**：比如“爱因斯坦”和现实生活中的朋友都被放进“人物”，缺少个人知识库需要的分层。
- **关系缺失**：大部分关系停留在“原始文档 -> 概念”，人物、事件、观念之间的关系没有建立。
- **别名识别失败**：同一个人或概念在不同笔记里的不同称呼没有被合并，导致关系断裂。

### 4. 第三阶段：加入约束、模板和背景知识

为了解决 Agent 偷懒问题，我开始加入三类东西：

1. **约束，也就是 harness**：明确什么必须抽、怎么分类、怎么验收。
2. **概念定义和模板**：不同类型的实体使用不同模板，迫使模型多维度收集信息。
3. **背景知识补齐**：补充别名、分类规则、个人长期关注主题。

#### 4.1 定义分类规则

优化前，实体分类非常粗糙，只有人物、城市、组织等少数类别，很多对象交织在一起。

优化后，实体分类开始贴合个人知识库本身，比如：

- 亲密关系
- 家庭成员
- 朋友
- 导师或思想影响者
- 工作相关人物
- 长期关注的思想家
- 城市与生活地点
- 重要事件
- 思维模型

图片占位：

- 优化前实体分类截图
- 优化后实体分类截图

#### 4.2 为不同类型定义模板

默认抽取标签太泛，信息密度低。引入模板后，每类知识都有明确采集面。

例如事件模板会要求记录：

- 时间
- 地点
- 参与人物
- 起因
- 过程
- 结果
- 情绪与判断
- 对后续人生的影响
- 相关原始笔记引用

人物模板会要求记录：

- 人物身份
- 与我的关系
- 关键互动
- 影响我的方式
- 共同经历
- 关系变化
- 未完成事项
- 别名与称呼

图片占位：

- 默认标签截图
- 定义后的标签截图
- 事件模板截图
- 人物实体模板截图

#### 4.3 加入别名和个人化分类

个人知识库不能只依赖通用分类。每个人都有自己长期追踪的主题。

比如我是芒格迷，所以“思维模型”就应该成为我的个人 wiki 中的一级视角。它不只是一个外部知识分类，而是我理解经历、决策和行为模式的核心框架。

因此，我结合自己的经历和查理·芒格的思维模型，建立了一个“我的思维模型视角”，用来持续追踪：

- 哪些经历体现了某个思维模型？
- 哪些错误来自某种认知偏差？
- 哪些长期选择体现了我的价值排序？
- 哪些观念对我产生了持续影响？

图片占位：

- 思维模型目录截图
- 分类归并截图

### 5. 第四阶段：让 wiki 可维护、可追踪

个人 wiki 不应该只是“当前认知”的静态快照，而应该是一个可追踪系统。

我开始为 wiki 增加：

- 状态追踪
- 维护手册
- 更新流程
- Agent 操作规范
- `AGENTS.md` / `agent.md` 类的维护约束

维护流程的目标是：每次新增、修改、归并知识，都能留下可检查的过程，而不是让 Agent 随意写入。

图片占位：

- 状态追踪截图
- 维护手册截图
- 维护流程示例截图
- agent.md 截图

### 6. 消费效果：从 RAG 到更懂我的知识库

#### 问题一：分析我人生中的重要事件

原始 RAG 往往更像关键词统计：找日期、找高频词、找相似片段。

llm-wiki 的回答则可以基于时间线、人物、事件、主题关系展开，给出更接近“人生结构”的分析。

图片占位：

- 原始 RAG 回答截图
- llm-wiki 回答截图

#### 问题二：分析查理·芒格对我的影响

普通 RAG 主要搜索“芒格”相关关键词，回答容易平铺直叙。

llm-wiki 不只查关键词，还能沿着已有关系扩展：

> 芒格 -> 理性 -> 某些具体经历 -> 某些价值判断

因此，回答中的引用更多，关联也更深。

图片占位：

- 关键词检索式回答截图
- llm-wiki 基于关系扩展的回答截图
- 引用增多截图

### 7. 第一章小结

个人知识库的构建让我得到三个结论：

1. **Agent 能力很重要**：构建个人知识库不是一次性转换，而是不断交互调优的过程。
2. **约束和模板是必要条件**：没有明确约束，Agent 会偷懒，内容会丢失。
3. **背景知识是壁垒**：真正有价值的不是文档数量，而是长期沉淀下来的个人语境、别名、关系和判断框架。

---

## 第二章：当方法论遇上复杂业务

### 1. 从个人 wiki 到数据 wiki

个人知识库构建完成后，我开始思考：这套方法能否复制到团队，解决更大规模的知识治理问题？

数据知识的落地，比个人知识库更难，因为它要求：

- 更强的业务约束
- 更专业的概念定义
- 更高的准确率
- 更稳定的消费链路
- 更可审计的维护过程

总结下来，它不是一个简单的文档整理问题，而是一个 **harness + evals 工程**。

### 2. 前置准备：三要素迁移

个人 wiki 的三要素可以迁移到业务场景：

| 要素 | 个人知识库 | 业务知识库 |
| --- | --- | --- |
| Agent 能力 | Codex + llm-wiki | Codex / claw / data-brain agent |
| 约束和模板 | 人物、事件、思维模型模板 | 数据知识框架、归因框架、指标口径模板 |
| 背景知识 | 日记、别名、个人长期主题 | 业务文档、数据口径、分析经验、历史 Case |

业务知识可以直接放到 raw 层，再由 Agent 按框架抽取。

### 3. 电商场景：初步验证

在电商场景中，wiki 初步构建后，用户反馈：

- 概念、实体、业务过程是有用的。
- 模型也挖出了一些无用分类，例如 projects、concerns、topic。
- 有些概念定义不清晰，比如数据集、Hive 表被放到了 entities 下面。

图片占位：

- 电商 wiki 初版结构截图

这个阶段说明：方法论有效，但默认抽取结果还不能直接生产使用，必须加入更强约束。

### 4. 剪映场景：Agent 在复杂任务中全方位偷懒

把框架复用到剪映场景后，问题变得更典型。

虽然规范已经定义得比较清楚，但在只有 13 篇文档的情况下，即使是 Codex 这种能力较强的 Agent，抽取结果依然不符合预期。

主要问题：

- 实体和概念抽取非常少。
- 规范里明确定义的通用范式没有被完整抽取。
- 双链丢失严重，13 篇文档之间建立的关系非常少。

图片占位：

- 抽取实体过少截图
- 规范中定义的通用范式截图
- 实际只抽出一个范式的截图

这个阶段的结论很直接：Agent 在复杂抽取项目中会全方位偷懒。

### 5. 调优方式：从“PUA 大模型”到工程化约束

当时的解决方式很原始：不断提醒模型“你又偷懒了，重新检查，继续补全”。这个过程中并没有修改规范，主要靠反复鞭策模型完成抽取。

它最后确实能工作，但这不是一个可规模化的方法。

这说明我们真正需要的不是更会骂模型，而是：

- 更明确的阶段验收
- 更强的输出格式约束
- 更可自动检查的质量标准
- 更稳定的 evals

图片占位：

- 抽得好的页面截图
- 需要调整的页面截图

### 6. 消费验证：为什么接入 wiki 后效果反而下降？

在消费场景中，第一次接入 wiki 后效果反而下降。

原因主要有三个：

1. **模型过度 follow 命中的知识**：命中什么就跟什么，反而丢掉原本的灵活性。
2. **重要知识没有被召回**：有些关键知识因为与 query 字面不匹配，就没有被检索到。
3. **隐含知识没有被提取**：例如“看任何模块 DAU 前都要先看大盘 DAU”这种业务常识，不会自然出现在检索结果中。

解决手段：

- **知识构建分层**：定义目录结构和知识层级，让 Agent 知道哪些是框架、哪些是事实、哪些是案例。
- **提升知识覆盖度**：补充隐含知识，人工 review 重要知识缺口，例如解释度、指标口径等。
- **定义消费链路**：明确消费路径、知识重要度和引用要求，要求 Agent 输出引用内容，提高 follow 的可控性。

图片占位：

- 接入 wiki 后效果下降截图
- 消费链路优化后效果截图

### 7. 第二章小结

业务知识库的落地暴露出三个核心问题：

1. **知识覆盖度问题**：重要知识不一定会被自动抽出来。
2. **业务常识问题**：很多真正关键的判断规则是隐含的。
3. **稳定性和维护问题**：靠人工反复催促 Agent，无法规模化。

因此，业务 wiki 的本质是一个知识工程系统，而不只是文档整理系统。

---

## 第三章：告别手工作坊，走向体系化知识生产

### 1. 为什么需要体系化

在业务场景中，wiki 已经证明有价值，但也暴露出一个现实问题：构建过程太依赖个人经验和手工调优，难以复制和推广。

要规模化，需要把经验固化成系统：

- 用 SKILL 规范知识生产流程。
- 用路由系统决定什么时候调用哪个 skill。
- 用质量保障体系防止 skill 变乱。
- 用结构化知识系统承载检索、图谱、证据和权限。

### 2. 用 SKILL 规范化知识生产

SKILL 可以理解为 Agent 的 SOP。它告诉 Agent：

- 什么时候使用这个能力。
- 应该按什么阶段执行。
- 输出必须长什么样。
- 哪些行为明确禁止。
- 如何验证结果合格。

一个标准 SKILL 结构可以是：

```yaml
---
name: meeting-ingestion
version: 1.0.0
description: |
  什么时候使用这个 skill，以及它解决什么问题。
triggers:
  - "meeting transcript"
  - "process this meeting"
tools:
  - search
  - query
  - put_page
mutating: true
writes_pages: true
writes_to:
  - meetings/
  - people/
---
```

正文至少包含：

- **Contract**：这个 skill 保证什么。
- **Phases**：分阶段流程和每阶段验收标准。
- **Output Format**：最终输出格式和必须交付的证据。
- **Anti-Patterns**：明确禁止哪些偷懒行为。
- **Verification**：如何检查结果合格。

### 3. SKILL 分层与路由

一个完整 wiki 系统中的 skill 可以分层：

#### 3.1 Always-on skills

所有消息都要经过的基础 skill，例如：

- 检查用户输入是否有 wiki 价值。
- 任何 wiki 操作都必须遵守操作规范。
- 读写知识库时自动检查引用、链接、目录和 schema。

#### 3.2 Brain operations

定义知识库的基础操作：

- 查询和读取
- 关系图查询
- 创建和丰富实体页
- 文件归档
- 引用修复
- 结构化研究
- 数据跟踪
- 发布分享
- Frontmatter lint

#### 3.3 Content & media ingestion

处理外部数据输入。

可以拆成两层：

- 总入口 ingest：判断输入类型，并定义所有 ingestion 的共同底线，例如保留原始资料、事实必须有 citation、实体必须 backlink。
- 具体输入源 skill：飞书文档、表格、会议纪要、网页、音视频等。

#### 3.4 Thinking skills

用于思考、判断、复盘、排错。

这类 skill 可以先不做，但从一些 Company Brain 的实现可以看出，成熟系统会把这些能力融入日常工作流。

#### 3.5 Operational skills

让系统持续运行、可维护、可调度：

- 查看运行中的 job
- 检查系统健康状态
- 创建或改进 skill
- 将粗糙流程正规化为 skill
- 验证 skill 和测试状态

#### 3.6 Setup & migration

负责初始化、迁移和健康维护：

- 外部系统迁移
- 链接检查
- 引用检查
- 孤儿页检查
- stale 信息检查
- backlink 检查
- 周期性综合和整理

#### 3.7 Identity & access

处理权限、身份和用户记忆：

- skill 操作权限
- 用户偏好
- 安全边界
- 可写目录限制

### 4. SKILL 调用约束

skill 多了以后，系统容易混乱，所以必须有全局规则。

#### 4.1 MECE

Mutually Exclusive, Collectively Exhaustive。

基本要求：

- 一个实体类型只能有一个 owner skill。
- 一个信号来源只能有一个 owner skill。
- 两个 skill 不应该同时负责创建同一种页面。
- 通过 ownership 表维护边界。

#### 4.2 Filing Rules

写入位置由内容的 primary subject 决定，不由格式、来源或当前运行的 skill 决定。

这可以防止 skill 随意把内容写进 wiki 目录。

#### 4.3 小样本原则

大批量任务必须先小样本验证：

1. 手动跑 3-10 个真实样本。
2. 检查输出质量和边界情况。
3. 用户确认质量。
4. 再写成 skill。
5. 最后加入 cron 或自动化流程。

#### 4.4 mutating 与 writes_pages

通过元数据标记高风险行为：

- `mutating: true` 表示 skill 会改变系统状态。
- `writes_pages: true` 表示 skill 会写知识页面。

有了这两个标记，就可以设置更严格的约束：

- 必须声明 `writes_to`。
- `writes_to` 必须在合法目录列表里。
- 必须遵守 citation、backlink、schema 规则。
- 必要时要求人工审核。
- 可以限制 Agent 只能写 drafts 或 generated。

### 5. 质量保障体系

#### 5.1 SKILL 评测

Resolver 测试用于防止 skill 系统混乱：

- `RESOLVER.md` 引用的 skill 文件必须存在。
- manifest 中的 skill 必须能从 resolver 触达。
- resolver trigger 和 skill frontmatter trigger 能 round-trip。
- 检测 MECE overlap。
- 检测没有 trigger 的 skill。
- 检测 citation、backlink、notability 等通用规则是否被重复写在各个 skill 中，而不是引用 convention。

#### 5.2 Skill 质量评测

所有 skill 必须包含：

- Contract
- Phases
- Output Format
- Anti-Patterns
- Verification

这解决的是“skill 写得随意、没有质量栏”的问题。

#### 5.3 路由评测

通过 routing eval 防止路由错误。

示例：

```json
{"intent":"把这批原始材料整理成公司页面","expected_skill":"wiki-entity-synthesis"}
{"intent":"检查这些页面有没有断链","expected_skill":"wiki-link-audit"}
{"intent":"今天构建 wiki","expected_skill":null}
```

#### 5.4 生成质量评测

对 skill 执行结果做输出检查：

- compiled truth 段落是否有 citation。
- 内部链接是否存在。
- backlink 是否完整。
- compiled truth 和 timeline 是否分隔清楚。
- 数据场景中的指标口径、归因链路、业务解释是否合格。

### 6. 不止于文档：知识系统的结构化底座

Markdown 适合人和 Agent 读写，但如果要支持检索、图谱遍历、证据追踪和权限管理，就需要把 wiki 从“文件集合”升级成“结构化知识系统”。

核心对象包括：

- `sources`：知识来源和仓库边界。
- `pages`：wiki 的核心实体。
- `content_chunks`：可检索切片和 embedding。
- `links`：页面之间的知识图谱关系。
- `timeline_entries`：事实发生的时间线。
- `raw_data`：原始证据。
- `files`：附件和媒体文件。
- `page_versions`：页面版本历史。
- `minion_jobs`：后台 Agent 任务状态。

### 7. 知识实体结构：当前结论与证据历史分离

一个关键设计是把 **Compiled Truth** 和 **Timeline** 分开。

读的时候先看当前结论，需要追溯时再看证据历史。

#### 7.1 YAML frontmatter：结构化身份层

```yaml
---
type: person | company | project | deal | concept
title: 实体显示名
aliases:
  - 别名
  - 邮箱
  - handle
tags:
  - 可检索分类
company: 当前公司
relationship: 你和它的关系
created: YYYY-MM-DD
updated: YYYY-MM-DD
validate: true
---
```

#### 7.2 Compiled Truth：当前认知层

典型人物实体页可以包含：

- Executive Summary：30 秒内理解这个实体当前意义。
- State：当前事实状态。
- Beliefs / What They Believe：这个人的判断和世界观。
- What They're Building：正在推进什么。
- Assessment：我的判断、机会、风险和下一步价值。
- Relationship：我们之间的历史和互动质量。
- Network：相关人、公司、投资人、同事和合作者。
- Open Threads：未关闭事项。

#### 7.3 Timeline：证据层

```markdown
## Timeline

- **YYYY-MM-DD** | 发生了什么。[Source: ...]
- **YYYY-MM-DD** | 新证据、新互动、新事实。[Source: ...]
```

### 8. 统一操作接口与混合检索

要让 Agent 稳定维护知识库，需要把底层能力包装成统一操作接口。

每个 operation 都应该包含：

- name
- description
- params
- handler
- scope
- localOnly
- cliHints

这些操作共同覆盖：

- page CRUD
- search / query
- tags
- links
- timeline
- jobs
- files
- takes
- sources

`BrainEngine` 更像数据库能力接口，而 `operations.ts` 负责定义 Agent 和用户看到的产品语义。

例如 `put_page` 不只是写 DB，还应该完成：

- 导入 Markdown
- 切 chunk
- 生成 embedding
- 处理 tags
- 按信任边界决定是否自动抽 links / timeline
- 运行 post-write lint

检索上，需要支持混合检索：

- 向量检索
- 关键词检索
- grep
- SQL 图关系检索

### 9. 最终设想：知识库成为 Agent 的外挂大脑

参考方向：

- crewlet.io
- loopany：https://github.com/superdesigndev/loopany
- Company Brain：组织级集体记忆

一个典型业务流程可以是：

1. 汽车客户在 Google Ads 上投放广告，系统推荐很多关键词。
2. Agent 理解客户点击事件，持续累积 signal。
3. 系统沉淀 task：“汽车保养”关键词作用不大，建议降低投放比例。
4. 观察 task 执行后的广告收益，发现无明显下降。
5. 沉淀 skill：对于汽车网店，屏蔽“汽车保养”相关关键词是有效的。

这时知识库不再只是文档仓库，而是 Agent 的长期记忆、判断依据和行动反馈系统。

---

## 结尾：从笔记到知识生产线

这次探索可以总结成一句话：

> llm-wiki 的价值，不是把文档变多，而是把零散文本变成可追踪、可检索、可治理、可复用的活知识系统。

从个人知识库到业务知识库，再到规模化知识生产线，我看到的关键路径是：

1. **先用 Agent 把知识跑起来**：让沉睡的文档重新产生连接和解释。
2. **再用约束和模板提高质量**：让 Agent 少偷懒，少丢信息，少乱分类。
3. **最后用 SKILL、evals 和结构化系统规模化**：让知识生产不依赖个人手工调优，而能被复制、治理和持续演化。

最终目标是让知识库成为 Agent 的外挂大脑：既能记住过去，也能理解现在，还能持续帮助未来的判断和行动。
